import { Title } from '@/shared/ui';
import * as styles from './SwiperContainer.module.scss';

export const SwiperContainers = () => {
    return (
        <section className={styles['swiper-containers']}><Title content='Исторические даты' /></section>
    )
}
